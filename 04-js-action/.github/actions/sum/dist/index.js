// PLACEHOLDER — gere com `npm install && npm run build` dentro de .github/actions/sum/
// Esse arquivo DEVE ser substituído pelo bundle real produzido pelo ncc antes de commitar.
// Sem o bundle real a action falhará no runner com "Cannot find module '@actions/core'".
console.error("dist/index.js não foi bundlado. Rode: npm install && npm run build");
process.exit(1);
